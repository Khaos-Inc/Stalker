;for a dltx file do configs\items\settings\mod_magnifier_switch_mappings_(whatever).ltx
;fields should be scope sections, pairs of magnifier off and magnifier on (order doesn't matter)
;scope_sec_off = scope_sec_on
;[magnifier_mappings]

  
;fields should be whole weapon sections, pairs of magnifier off and magnifier on (order doesn't matter)
;has priority over the scope-to-scope mappings
;section_off = section_on
![unique_mappings]
wpn_xm4_admin_off		= wpn_xm4_admin
